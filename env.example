# Database
MONGODB_URI=mongodb://localhost:27017/task-management
# or use MongoDB Atlas connection string
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/task-management

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRE=30d

# Server Configuration
PORT=5000
NODE_ENV=development

# Email Configuration (Resend)
RESEND_API_KEY=re_your_resend_api_key_here
EMAIL_FROM=onboarding@resend.dev
# For production, use your verified domain:
# EMAIL_FROM=noreply@yourdomain.com

# Website URL (used in email links)
WEBSITE_URL=https://bxiflowdesk.netlify.app/login

# Email Service Configuration
EMAIL_SERVICE=resend
# Alternative: EMAIL_SERVICE=smtp (if you want to use nodemailer instead)

